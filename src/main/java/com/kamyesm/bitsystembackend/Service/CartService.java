package com.kamyesm.bitsystembackend.Service;

public class CartService {
    // main methods
    // add to cart (registered and anonymous user)
    // delete from cart (registered and anonymous user)
    // change quantity (registered and anonymous user)
    // fetch current cart (registered and anonymous user)
    // connect cart to user
    // disconnect cart from user
}
